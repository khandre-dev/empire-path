<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Culinary Path of Empires</title>

<style>
/* ---------- RESET ---------- */
{
margin: 0;
padding: 0;
box-sizing: border-box;
}

/* ---------- INTRO ---------- /
#intro {
  position: fixed;
  inset: 0;
  background: black;
  z-index: 1000;
}

/ Video fills screen /
#intro video {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/ Enter button /
#enterBtn {
  position: absolute;
  bottom: 12%;
  left: 50%;
  transform: translateX(-50%);
  padding: 1rem 2.8rem;
  background: rgba(255, 230, 170, 0.9);
  border: none;
  border-radius: 40px;
  font-size: 1.1rem;
  letter-spacing: 0.18em;
  cursor: pointer;
}

/ ---------- MAIN CONTENT ---------- */
#menu {
  display: none;
  min-height: 100vh;
  background: linear-gradient(#efe2c6, #c9b58c);
  padding: 3rem 1.5rem;
}
</style>
</head>

<body>

<!-- INTRO -->
<div id="intro">
  <video autoplay muted playsinline loop>
    <source src="doors.mp4" type="video/mp4">
    Your browser does not support the video tag.
  </video>

  <button id="enterBtn" onclick="enterSite()">ENTER</button>
</div>

<!-- MENU / CONTENT -->
<div id="menu">
  <h1>Welcome to the Culinary Path</h1>
  <p>This is where your empires and menu content go.</p>
</div>

<script>
function enterSite() {
  const intro = document.getElementById("intro");
  const menu = document.getElementById("menu");

  intro.style.opacity = "0";

  setTimeout(() => {
    intro.style.display = "none";
    menu.style.display = "block";
  }, 600);
}
</script>

</body>
</html>
